@{
    // everything here applies to **all** razor pages; the typical use is to set the default layout
    Layout = "~/Views/Shared/Site.cshtml";
    
    // this is VERY IMPORTANT; do **NOT** leave any white-space after the closing brace,
    // it **will** break any views that output xml, as the xml declaration will be down a line
}