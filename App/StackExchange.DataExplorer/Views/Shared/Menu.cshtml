@model IEnumerable<StackExchange.DataExplorer.ViewModel.SubHeaderViewData>
@using StackExchange.DataExplorer
@using StackExchange.DataExplorer.ViewModel

<div id="hmenus">
    <div class="nav">
        <ul>
            @foreach (SubHeaderViewData item in Model.Where(i=>!i.RightAlign))
            {
                <li>
                    <a 
                    @if (item.Id.HasValue())
                    {
                        <text>id="@item.Id"</text>    
                    }
                    @if (item.Selected)
                    {
                        <text>class="youarehere"</text>
                    }
                    href="@item.Href" title="@item.Title">@item.Description</a>
                </li>                
            }
        </ul>
    </div>
    <div style="float:right;" class="nav">
        <ul>
            @foreach (SubHeaderViewData item in Model.Where(i=>i.RightAlign))
            {
                <li style="margin-right:0px;">
                    <a
                    @if (item.Id.HasValue())
                    {
                        <text>id="@item.Id"</text>    
                    }
                    @if (item.Selected)
                    {
                        <text>class="youarehere"</text>
                    }
                    href="@item.Href" title="@item.Title">@item.Description</a>
                </li>
            }
        </ul>
    </div>
</div>